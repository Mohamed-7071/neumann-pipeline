#ifndef PIPELINE_RUN_H
#define PIPELINE_RUN_H

#include <stdint.h>

// If you want to expose a function to run the pipeline, declare it here
// void run_pipeline(const char *filename);

#endif // PIPELINE_RUN_H