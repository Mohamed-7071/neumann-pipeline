

el haga saved fel memory as in hexa bas fel print used func to print in binary. ask ta mafrod saved ezay.

now we need to start fel pipeline --- fetch, decode, exec, mem, write back
check el pdf 3alashan mosh same implementation zai el lecs w mosh fully capable of parallel pipelinning 
("You can not have the Instruction Fetch (IF) and Memory (MEM) stages working in parallel. Only one
of them is active at a given clock cycle.")

also regs not initialized most prob hane7tag n3ml el logic w  m7tageen 0 reg.